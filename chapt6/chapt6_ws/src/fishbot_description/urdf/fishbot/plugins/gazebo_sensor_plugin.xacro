<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro">
    <!-- Gazebo Sim uses ros_gz_bridge for sensor data, no plugins needed -->
    <xacro:macro name="gazebo_sensor_plugin">
    </xacro:macro>
</robot>
